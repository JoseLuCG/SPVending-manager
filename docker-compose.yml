version: "3.9"
services:
  frontend:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: frontend
    restart: always
    ports:
      - 5173:5173
    volumes:
      - /app
      - /app/node_modules
    environment:
      - VITE_APP_BACKEND_ADDRESS=http://localhost:8080

